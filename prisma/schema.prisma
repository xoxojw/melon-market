// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// ex) User DB : { id: 1, username: john }
// Comments DB에 { id: 1, text: wow!, user: 5 }로 등록하려하면 에러가 발생하지 않음 (user: 5가 없음에도)
// 일반적인 sql이나 postgresql 환경에서는 외래키 제약때문에 에러 발생함
// comments DB에 user라는 외래키가 있으니 User 테이블에서 검색하기 때문
// 하지만 이번에 사용하는 Vitess는 foreign key constranint를 지원하지 않음
// -> 따라서 이러한 확인작업을 DB side에서 하는 것이 아니라 prisma를 통해서 할 것

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider     = "mysql"
  url          = env("DATABASE_URL")
  // relationMode 설정: 데이터 간 관계를 관리할 때 prisma 설정대로 하겠다는 의미
  // 참조 무결성
  relationMode = "prisma"
}

// npx prisma database push
model User {
  id        Int      @id @default(autoincrement())
  phone     Int?     @unique
  email     String?  @unique
  name      String
  avartar   String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
